# Reminder for tomorrow

- **Introslider scrollbar:** If the pink scrollbar still doesn’t show in Safari on wide screens, add a custom scrollbar (HTML + JS) because Safari can ignore CSS scrollbar styling.

*(Edit this file if the reminder was for something else.)*
